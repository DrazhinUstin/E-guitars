import React from 'react';
import { HeroSlider, Featured, SubscribeForm, Brands } from '../components';

const HomePage = () => {
    return (
        <>
            <HeroSlider />
            <Featured />
            <SubscribeForm />
            <Brands />
        </>
    );
};

export default HomePage;
