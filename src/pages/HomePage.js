import React from 'react';
import { HeroSlider, Featured } from '../components';

const HomePage = () => {
    return (
        <>
            <HeroSlider />
            <Featured />
        </>
    );
};

export default HomePage;
